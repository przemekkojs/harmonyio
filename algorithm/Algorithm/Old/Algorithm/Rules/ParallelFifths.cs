using Algorithm.Old.Music;
using Algorithm.Old.Algorithm;

namespace Algorithm.Old.Algorithm.Rules
{
    public class ParallelFifths : Rule
    {
        public ParallelFifths() : base("Parallel Fifths", "") { }

        public override bool IsSatisfied(params Stack[] stacks)
        {
            throw new NotImplementedException();
        }
    }
}
